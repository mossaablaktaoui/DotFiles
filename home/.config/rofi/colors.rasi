/* Compatibility aliases for older Rofi menus. */
@import "~/.config/liquid-glass/palette.rasi"

* {
    background: @lg-glass;
    surface: @lg-surface;
    selected: @lg-hover;
    text: @lg-text;
    subtext: @lg-dim;
    selected-text: @lg-text;
    border: @lg-border;
}
